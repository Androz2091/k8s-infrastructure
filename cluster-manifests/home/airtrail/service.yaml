apiVersion: v1
kind: Service
metadata:
  name: airtrail
  namespace: home
  labels:
    app: airtrail
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app: airtrail
