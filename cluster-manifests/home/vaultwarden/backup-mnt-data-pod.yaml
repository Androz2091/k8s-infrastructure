apiVersion: v1
kind: Pod
metadata:
  name: backup-vaultwarden-mnt-data-pod
  namespace: home
spec:
  containers:
  - name: debug
    image: busybox:latest
    command:
      - sleep
      - "infinity"
    volumeMounts:
    - name: vaultwarden-data
      mountPath: /mnt/data
  volumes:
  - name: vaultwarden-data
    persistentVolumeClaim:
      claimName: vaultwarden-data-pvc
