helmCharts:
  - name: pgadmin4
    releaseName: pgadmin4
    namespace: db
    version: 1.13.6
    repo: https://helm.runix.net
    valuesInline:
      env:
        email: smnlefort@gmail.com
      envVarsFromSecrets:
        - name: pgadmin-secrets
          key: password

namespace: db

resources:
  - sealed-secrets.yaml

patches: []
