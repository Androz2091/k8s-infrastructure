apiVersion: v1
kind: Service
metadata:
  name: manage-invite-bot
  labels:
    app: manage-invite-bot
spec:
  ports:
  - port: 3200
    protocol: TCP
    targetPort: http
  selector:
    app: manage-invite-bot
  type: ClusterIP
